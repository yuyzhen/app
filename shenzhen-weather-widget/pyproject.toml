[project]
name = "shenzhen-weather-widget"
version = "1.0.0"
description = "A lightweight Windows desktop weather widget for Shenzhen."
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pyinstaller]
name = "ShenzhenWeatherWidget"
